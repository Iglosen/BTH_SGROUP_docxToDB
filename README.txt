Alla tabeller innehåller ett ID.

Backslashes kan inte läsas in utav någon anledning, även när paths läggs in som strängar så kommer inte "\" med.

Checkboxar läses inte in

För att köra appen krävs en databas vid namn reportbase. Setup för denna finns i "sql/setup.sql"

För att starta appen så kan man antingen skriva "node index" i en node-js-terminal eller klicka på docxToDatabase.exe

Min stylesheet syns däremot inte på .exe-filen